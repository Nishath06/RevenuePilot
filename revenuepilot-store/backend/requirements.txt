# =============================================================================
# RevenuePilot Store Backend — Python Dependencies
#
# SECURITY PINS (must match Dockerfile and ci.yml env vars):
#   setuptools, wheel, msgpack, jaraco.context are pinned to specific versions
#   to remediate Trivy HIGH/CRITICAL CVEs. Do NOT remove or relax these pins.
#   Update them only when a newer patched version is available.
# =============================================================================

# ── Security Patches (pinned — DO NOT remove or relax) ─────────────────────
# These MUST appear first so pip resolves them before transitive dependencies.
setuptools==84.0.0
wheel==0.46.2
msgpack==1.2.1
jaraco.context==6.1.2

# ── FastAPI & ASGI ───────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# ── MongoDB (async) ──────────────────────────────────────────────────────────
motor>=3.3.2,<3.7.0
pymongo>=4.6.0,<4.10.0
beanie>=1.25.0

# ── Settings & Validation ────────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1

# ── Payment ──────────────────────────────────────────────────────────────────
razorpay>=1.4.1

# ── HTTP & Form Parsing ──────────────────────────────────────────────────────
python-multipart>=0.0.9
email-validator>=2.1.0

# ── HTTP Client ──────────────────────────────────────────────────────────────
httpx>=0.27.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.5
