[pytest]
testpaths = ../tests
python_files = test_*.py
asyncio_mode = auto

markers =
    unit: Fast unit tests (no external services)
    integration: Requires external services (DB, webhooks, etc.)
    razorpay: Requires Razorpay credentials
